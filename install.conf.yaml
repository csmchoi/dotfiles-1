- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.zshrc: '.zshrc'
    ~/.vimrc.before: '.vimrc.before'

- shell:
  - [git submodule update --init --recursive, Installing submodules]
